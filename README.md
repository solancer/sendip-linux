sendip-linux
============

This bash script will mail your Linux system IP address

You can add this script to startup or as a cron job on the system, every time your computer reboots you will get the new IP address.

For extracting internet IP address use: curl ifconfig.me
